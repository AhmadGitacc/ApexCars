
---
layout: layout.liquid
---

<!-- Hero Section -->
<section class="relative h-[80vh] overflow-hidden bg-black flex items-end pb-24">
  <img src="https://picsum.photos/id/1071/1920/1080" alt="Hero Image" class="absolute inset-0 w-full h-full object-cover opacity-60 grayscale hover:grayscale-0 transition duration-1000">
  <div class="absolute inset-0 hero-gradient"></div>
  
  <div class="container mx-auto px-6 relative z-10">
    <span class="inline-block px-3 py-1 bg-white text-black text-[10px] font-bold uppercase tracking-[0.2em] mb-6">Featured Issue</span>
    <h1 class="text-5xl md:text-8xl text-white font-black tracking-tighter leading-none mb-6">
      THE SILENCE OF<br>ENGINEERING.
    </h1>
    <p class="text-zinc-300 text-lg md:text-xl max-w-xl font-light">
      Exploring the delicate balance between brutal performance and minimalist aesthetics in modern grand touring.
    </p>
  </div>
</section>

<!-- Journal Grid -->
<section class="container mx-auto px-6 py-24">
  <div class="flex flex-col md:flex-row justify-between items-baseline mb-16 border-b border-zinc-200 pb-6">
    <h2 class="text-3xl font-black tracking-tighter uppercase">Recent Entries</h2>
    <p class="text-zinc-400 text-sm font-medium">CURATED AUTOMOTIVE STORIES</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-12 gap-y-20">
    {% for post in collections.posts %}
      <article class="group">
        <a href="{{ post.url }}" class="block overflow-hidden mb-6 bg-zinc-200">
          <img src="{{ post.data.coverImage }}" alt="{{ post.data.title }}" class="w-full aspect-[4/5] object-cover transition duration-700 group-hover:scale-105 group-hover:opacity-90 grayscale hover:grayscale-0">
        </a>
        <div class="space-y-3">
          <div class="flex items-center space-x-3 text-[10px] font-bold uppercase tracking-widest text-zinc-400">
            <span>{{ post.data.category }}</span>
            <span class="w-1 h-1 bg-zinc-300 rounded-full"></span>
            <time>{{ post.date | postDate }}</time>
          </div>
          <h3 class="text-2xl font-bold leading-tight group-hover:text-zinc-600 transition">
            <a href="{{ post.url }}">{{ post.data.title }}</a>
          </h3>
          <p class="text-zinc-500 text-sm leading-relaxed line-clamp-2">
            {{ post.templateContent | strip_html | truncate: 120 }}
          </p>
        </div>
      </article>
    {% endfor %}
  </div>
</section>
