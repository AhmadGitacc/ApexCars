
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title | default: metadata.title }} | {{ metadata.title }}</title>
  <meta name="description" content="{{ description | default: metadata.description }}">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&family=Playfair+Display:ital,wght@0,700;1,700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
    h1, h2, h3, .serif { font-family: 'Playfair Display', serif; }
    .hero-gradient { background: linear-gradient(to bottom, rgba(0,0,0,0) 0%, rgba(0,0,0,0.8) 100%); }
    .prose img { border-radius: 0.25rem; }
  </style>
</head>
<body class="bg-zinc-50 text-zinc-900 selection:bg-zinc-900 selection:text-white">

  <!-- Header -->
  <header class="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-zinc-100">
    <nav class="container mx-auto px-6 py-4 flex justify-between items-center">
      <a href="/" class="text-2xl font-black tracking-tighter text-zinc-900">APEX.</a>
      <div class="space-x-8 text-xs font-semibold uppercase tracking-widest text-zinc-500">
        <a href="/" class="hover:text-zinc-900 transition {% if page.url == '/' %}text-zinc-900{% endif %}">Journal</a>
        <a href="/archive/" class="hover:text-zinc-900 transition {% if page.url == '/archive/' %}text-zinc-900{% endif %}">Archive</a>
        <a href="/about/" class="hover:text-zinc-900 transition {% if page.url == '/about/' %}text-zinc-900{% endif %}">About</a>
      </div>
    </nav>
  </header>

  <main>
    {{ content }}
  </main>

  <!-- Footer -->
  <footer class="bg-zinc-900 text-zinc-400 py-20 mt-20">
    <div class="container mx-auto px-6 text-center">
      <h2 class="text-3xl font-black tracking-tighter text-white mb-4">APEX</h2>
      <p class="text-sm max-w-xs mx-auto mb-8 opacity-60">
        {{ metadata.description }}
      </p>
      <div class="flex justify-center space-x-6 mb-12">
        <a href="{{ metadata.social.instagram }}" target="_blank" rel="noopener" class="hover:text-white transition">Instagram</a>
        <a href="{{ metadata.social.twitter }}" target="_blank" rel="noopener" class="hover:text-white transition">Twitter</a>
        <a href="{{ metadata.social.rss }}" class="hover:text-white transition">RSS</a>
      </div>
      <p class="text-[10px] uppercase tracking-widest">
        &copy; {{ "now" | date: "%Y" }} {{ metadata.author }}. All rights reserved.
      </p>
    </div>
  </footer>

</body>
</html>
